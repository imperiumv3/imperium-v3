# Landing Page

- `LandingPage.tsx` — page UI (HTML/JSX)
- `landing.logic.ts` — page logic (scroll, animations)
- `landing.css` — page styling (TBD; landing styles currently live in src/styles.css under the `.imp-*` namespace)
- `components/` — landing-only components
- `assets/` — landing-only images / 3D / sprites
